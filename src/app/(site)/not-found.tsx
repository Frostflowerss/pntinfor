import { NotFoundView } from "@/components/site/NotFoundView";

/** Bắt notFound() gọi từ trong nhóm (site) — có header, nav, footer. */
export default function NotFound() {
  return <NotFoundView />;
}
